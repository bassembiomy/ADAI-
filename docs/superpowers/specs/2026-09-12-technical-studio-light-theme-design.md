# Technical Studio Light Theme

The light theme uses a cool ivory canvas, white elevated surfaces, slate typography, emerald structural accents, and orange interaction accents. Theme tokens remain the single source of truth; compatibility selectors cover existing hard-coded utility classes so legacy panels also switch without changing behavior. SysML diagrams use a quiet blueprint grid, stronger block hierarchy, monospace semantic text, and high-contrast relationship strokes.
