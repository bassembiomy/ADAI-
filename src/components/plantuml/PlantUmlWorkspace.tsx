import React, { useMemo, useState } from 'react';
import { getSequencePaletteItems } from '../../features/plantuml/adapters/sequenceAdapter';
import { getUseCasePaletteItems } from '../../features/plantuml/adapters/useCaseAdapter';
import { validateVisualDiagram } from '../../features/plantuml/model/diagramValidation';
import type { VisualDiagramElement, VisualDiagramModel, VisualDiagramType } from '../../features/plantuml/model/visualDiagramModel';
import { PlantUmlCanvas } from './PlantUmlCanvas';
import { PlantUmlPalette } from './PlantUmlPalette';
import { PlantUmlPropertiesPanel } from './PlantUmlPropertiesPanel';
import { renderVisualDiagramToSvg } from '../../features/plantuml/rendering/offlinePlantUmlRenderer';

export function PlantUmlWorkspace({ diagram, onChange, onSave, onExport }: { diagram: VisualDiagramModel; onChange: (diagram: VisualDiagramModel) => void; onSave?: () => void; onExport?: () => void }) {
  const [selectedId, setSelectedId] = useState<string>();
  const [connectSourceId, setConnectSourceId] = useState<string>();
  const palette = useMemo(() => diagram.type === 'sequence' ? getSequencePaletteItems() : getUseCasePaletteItems(), [diagram.type]);
  const validation = validateVisualDiagram(diagram);
  const add = (kind: string, position = { x: 120 + diagram.elements.length * 24, y: 100 + diagram.elements.length * 20 }) => {
    const element: VisualDiagramElement = { id: `element-${Date.now().toString(36)}`, kind, label: palette.find((item) => item.kind === kind)?.label ?? kind, position, size: { width: 160, height: 72 }, style: {} };
    onChange({ ...diagram, elements: [...diagram.elements, element] }); setSelectedId(element.id);
  };
  const preview = renderVisualDiagramToSvg(diagram);
  const changeType = (type: VisualDiagramType) => { if (type !== diagram.type) { onChange({ ...diagram, type, title: type === 'sequence' ? 'New sequence diagram' : 'New use case diagram', elements: [], relationships: [] }); setSelectedId(undefined); setConnectSourceId(undefined); } };
  const connect = (sourceId: string, targetId: string) => { onChange({ ...diagram, relationships: [...diagram.relationships, { id: `relationship-${Date.now().toString(36)}`, kind: diagram.type === 'sequence' ? 'message' : 'association', sourceId, targetId }] }); setConnectSourceId(undefined); };
  return <section className="flex h-full min-h-[560px] flex-col overflow-hidden rounded-lg border border-slate-700 bg-slate-900 text-slate-100" aria-label={`${diagram.type === 'sequence' ? 'Sequence Diagram' : 'Use Case'} workspace`}><header className="flex flex-wrap items-center justify-between gap-3 border-b border-slate-700 bg-slate-950/80 px-4 py-3"><div><div className="mb-2 flex gap-2"><button type="button" onClick={() => changeType('sequence')} className={`rounded border px-3 py-1.5 text-[11px] font-semibold ${diagram.type === 'sequence' ? 'border-orange-300 bg-orange-500 text-slate-950' : 'border-slate-600 bg-slate-900 text-slate-300 hover:border-orange-400'}`}>Sequence Diagram</button><button type="button" onClick={() => changeType('use-case')} className={`rounded border px-3 py-1.5 text-[11px] font-semibold ${diagram.type === 'use-case' ? 'border-orange-300 bg-orange-500 text-slate-950' : 'border-slate-600 bg-slate-900 text-slate-300 hover:border-orange-400'}`}>Use Case</button></div><h2 className="text-sm font-semibold">{diagram.title || (diagram.type === 'sequence' ? 'Untitled sequence diagram' : 'Untitled use case diagram')}</h2><p className="text-[11px] text-orange-300">Interactive visual modeling · fully offline</p></div><div className="flex gap-2"><button type="button" onClick={() => setConnectSourceId(connectSourceId ? undefined : selectedId)} disabled={!selectedId && !connectSourceId} className={`rounded border px-3 py-1.5 text-xs ${connectSourceId ? 'border-orange-400 bg-orange-400/10 text-orange-200' : 'border-slate-600 text-slate-200 hover:border-orange-400'}`}>{connectSourceId ? 'Select target…' : 'Connect'}</button><button type="button" onClick={onSave} className="rounded bg-orange-500 px-3 py-1.5 text-xs font-semibold text-slate-950 hover:bg-orange-400">Save</button><button type="button" onClick={onExport} className="rounded border border-slate-600 text-slate-200 px-3 py-1.5 text-xs hover:border-orange-400">Export source</button></div></header><div className="flex min-h-0 flex-1"><PlantUmlPalette items={palette} onAdd={add} /><div className="grid min-w-0 flex-1 grid-cols-2"><PlantUmlCanvas diagram={diagram} selectedId={selectedId} onSelect={setSelectedId} onDrop={add} connectSourceId={connectSourceId} onConnect={connect} onMove={(id, position) => onChange({ ...diagram, elements: diagram.elements.map((item) => item.id === id ? { ...item, position } : item) })} /><div className="overflow-auto border-l border-slate-700 bg-[#111827] p-3" aria-label="Offline PlantUML preview"><div className="mb-2 text-[10px] uppercase tracking-wide text-slate-500">Rendered preview</div><div dangerouslySetInnerHTML={{ __html: preview }} /></div></div><PlantUmlPropertiesPanel diagram={diagram} selectedId={selectedId} onChange={(element) => onChange({ ...diagram, elements: diagram.elements.map((item) => item.id === element.id ? element : item) })} /></div><footer className="border-t border-slate-700 bg-slate-950/80 px-4 py-2 text-[11px] text-slate-400">{validation.valid ? 'Ready to render offline' : `${validation.issues.length} validation issue${validation.issues.length === 1 ? '' : 's'} · fix before export`}</footer></section>;
}
